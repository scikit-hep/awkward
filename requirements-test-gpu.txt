# The CI installs cudf, cupy, and cuda-cccl using conda
# If you are using this file manually uncomment the following lines and
# set the cuda version matching your system (currently supporting CUDA 12 and 13).
# cuda-cccl >= 1.0.2 is required: it fixes the per-call build-cache leak in
# cuda.compute.unary_transform that the CUDA reducer path relies on (PR #4056).
# >= 1.2.0 is required: that's the minimum cuda-cccl version that uses
# numba-cuda-mlir, which the CUDA backend now also requires.
# cudf-cu12
# cupy-cuda12x>=14
# cuda-cccl[cu12]>=1.2.0
fsspec>=2022.11.0
hypothesis-awkward==0.20.2
numba>=0.60
numba-cuda>=0.25.0  # backs the public `ak.numba.cuda` extension
numba-cuda-mlir>=0.5.2,<0.6  # required by the CUDA backend
# numpy<2.5 until a numba-cuda release supports numpy 2.5
# (NVIDIA/numba-cuda#907): numba-cuda 0.30.4 references the removed
# np.row_stack at import time
numpy<2.5
pyarrow
pytest>=6
PyYAML
uproot>=5
