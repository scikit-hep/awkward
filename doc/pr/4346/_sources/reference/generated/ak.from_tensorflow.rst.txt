ak.from_tensorflow
==================

Defined in `awkward.operations.ak_from_tensorflow <https://github.com/scikit-hep/awkward/blob/c0a230d71f7e18b7bcfc949ee426b8a6b8b78c01/src/awkward/operations/ak_from_tensorflow.py>`__ on `line 13 <https://github.com/scikit-hep/awkward/blob/c0a230d71f7e18b7bcfc949ee426b8a6b8b78c01/src/awkward/operations/ak_from_tensorflow.py#L13>`__.

.. py:function:: ak.from_tensorflow(array)


   Converts a TensorFlow Tensor into an Awkward Array.

   A tensor on a GPU is not copied: its buffer is shared through DLPack. A
   tensor on a CPU is copied, because a NumPy array is mutable and a
   TensorFlow tensor is not.

   If ``array`` contains any other data types the function raises an error.

   :param array: (TensorFlow Tensor):
                 Tensor to convert into an Awkward Array.

   :returns: An :py:obj:`ak.Array` built from the given TensorFlow Tensor.

