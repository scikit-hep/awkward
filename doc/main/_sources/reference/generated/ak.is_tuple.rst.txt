ak.is_tuple
===========

Defined in `awkward.operations.ak_is_tuple <https://github.com/scikit-hep/awkward/blob/6062e76d21b50e2ae83456bb793e00815e76af1e/src/awkward/operations/ak_is_tuple.py>`__ on `line 11 <https://github.com/scikit-hep/awkward/blob/6062e76d21b50e2ae83456bb793e00815e76af1e/src/awkward/operations/ak_is_tuple.py#L11>`__.

.. py:function:: ak.is_tuple(array)

   Returns True if a record, or the outermost record of an array, is a tuple.

   If ``array`` is a record, this returns True if the record is a tuple. If
   ``array`` is an array, this returns True if the outermost record is a tuple.

   :param array: Array-like data (anything :py:obj:`ak.to_layout` recognizes).

   :returns: True if ``array`` (or its outermost record) is a tuple, False otherwise.

