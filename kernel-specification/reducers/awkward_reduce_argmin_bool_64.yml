- name: awkward_reduce_argmin_bool_64
  args:
    - name: toptr
      type: List[int64_t]
      direction: out
    - name: fromptr
      type: Const[List[bool]]
      direction: in
      role: reducer-fromptr
    - name: parents
      type: Const[List[int64_t]]
      direction: in
      role: reducer-parents
    - name: lenparents
      type: int64_t
      direction: in
      role: reducer-lenparents
    - name: outlength
      type: int64_t
      direction: in
      role: reducer-outlength
  definition: |
    def awkward_reduce_argmin_bool_64(toptr , fromptr , parents , lenparents , outlength ):
        for k in range(outlength):
            toptr[k] = -1
        for i in range(lenparents):
            parent = parents[i]
            if (toptr[parent] == -1) or ((fromptr[i] != 0) < (fromptr[toptr[parent]] != 0)):
                toptr[parent] = i
  description: null