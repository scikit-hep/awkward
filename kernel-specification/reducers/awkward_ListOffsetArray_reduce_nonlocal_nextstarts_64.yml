- name: awkward_ListOffsetArray_reduce_nonlocal_nextstarts_64
  args:
    - name: nextstarts
      type: List[int64_t]
      direction: out
    - name: nextparents
      type: Const[List[int64_t]]
      direction: in
      role: reducer-nextparents
    - name: nextlen
      type: int64_t
      direction: in
      role: reducer-nextlen
  definition: |
    def awkward_ListOffsetArray_reduce_nonlocal_nextstarts_64(nextstarts , nextparents , nextlen ):
        lastnextparent = -1
        for i in range(nextlen):
            if nextparents[i] != lastnextparent:
                nextstarts[nextparents[i]] = i
            lastnextparent = nextparents[i]
  description: null