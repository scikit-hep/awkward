name: awkward-docs
channels:
  - conda-forge
dependencies:
  - pip
  - cxx-compiler
  - root
  - ipykernel!=6.18.0
  - pip:
    # [[[cog
    # import cog
    # # Inject requirements
    # with open("requirements.txt") as f:
    #     for line in f:
    #         if line.strip():
    #             cog.out(f"- {line}")
    # ]]]
    # [[[end]]]

