name: awkward-docs
channels:
  - conda-forge
dependencies:
  - pip
  - xeus-cling=0.13.0
  - gxx_linux-64=9.4.0
  - pip:
    # [[[cog
    # import cog
    # # Inject requirements
    # with open("requirements.txt") as f:
    #     for line in f:
    #         if line.strip():
    #             cog.out(f"- {line}")
    # ]]]
    # [[[end]]]

